% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BetterVis_Dotplot_Smoothline.R
\name{BetterVis_Dotplot_Smoothline}
\alias{BetterVis_Dotplot_Smoothline}
\title{Create a Scatter Plot with Smoothed Line and Correlation}
\usage{
BetterVis_Dotplot_Smoothline(
  data,
  x_var,
  y_var,
  classified_var,
  color = NULL,
  size_var = NULL,
  point_size = 15,
  smoothline = TRUE,
  smoothline_width = 1,
  smoothline_color = "#CE743C",
  cor_test = TRUE,
  cor_method = "spearman",
  cor_color = "black",
  cor_position = c(2, 2),
  title = NULL,
  x_title = NULL,
  y_title = NULL,
  legend = TRUE,
  legend_title = NULL,
  legend_position = "right",
  global_size_adjust = 1
)
}
\arguments{
\item{data}{A data frame containing the plotting data.}

\item{x_var, y_var}{Character strings specifying the column names for the continuous x and y-axis variables.}

\item{classified_var}{A character string for the column name of the categorical variable used for point fill color.}

\item{color}{(Optional) A vector of colors for the different levels of `classified_var`. If `NULL`, a default `paletteer` color scale is used.}

\item{size_var}{(Optional) A character string for a numeric column to be mapped to point size.}

\item{point_size}{Numeric. The base or maximum size of the points. Default is `15`.}

\item{smoothline}{Logical. If `TRUE`, adds a smoothed conditional mean line (`loess`). Default is `TRUE`.}

\item{smoothline_width}{Numeric. The line thickness of the smoothed line. Default is `1`.}

\item{smoothline_color}{Character string. The color of the smoothed line. Default is `"#CE743C"`.}

\item{cor_test}{Logical. If `TRUE`, displays correlation statistics on the plot. Default is `TRUE`.}

\item{cor_method}{Character string. The correlation method to use ("pearson", "spearman", or "kendall"). Default is `"spearman"`.}

\item{cor_color}{Character string. The color for the correlation text and label box. Default is `"black"`.}

\item{cor_position}{A numeric vector of length 2, `c(x, y)`, specifying the coordinates for the correlation label.}

\item{title, x_title, y_title}{Character strings for plot titles.}

\item{legend}{Logical. If `TRUE`, the legend is displayed. Default is `TRUE`.}

\item{legend_title}{Character string for the legend title.}

\item{legend_position}{Character string specifying the legend position (e.g., "right", "bottom"). Default is `"right"`.}

\item{global_size_adjust}{A single numeric value to uniformly scale all text elements. Default is `1`.}
}
\value{
A `ggplot` object.
}
\description{
This function generates a highly customized scatter plot using `ggplot2`. It
includes options for adding a smoothed regression line, displaying correlation
test results, and utilizes themes and palettes from `ggprism` and `paletteer`.
}
\examples{
if (requireNamespace("ggprism", quietly = TRUE) &&
    requireNamespace("paletteer", quietly = TRUE)) {
  # Load example data
  data("BetterVis_Dotplot_Smoothline_example", package = "BetterVis")

  # Example 1: Using a manual color palette and sizing points by a variable
  BetterVis_Dotplot_Smoothline(
    data = BetterVis_Dotplot_Smoothline_example,
    x_var = "Sepal.Width", y_var = "Sepal.Length", classified_var = "Species",
    size_var = "Petal.Length",
    color = c("#FBA72A", "#78B7C5", "#7294D4"),
    cor_position = c(3.8, 4.5)
   )

  # Example 2: More customization and using default paletteer colors
  BetterVis_Dotplot_Smoothline(
    data = BetterVis_Dotplot_Smoothline_example,
    x_var = "Sepal.Width", y_var = "Sepal.Length", classified_var = "Species",
    size_var = "Petal.Length",
    color = NULL, # Let paletteer choose colors
    point_size = 20,
    smoothline_width = 2,
    smoothline_color = "#FB8072",
    cor_method = "pearson",
    cor_position = c(3.8, 4.5),
    title = "Iris Sepal Dimensions",
    x_title = "Sepal Width",
    y_title = "Sepal Length",
    legend_title = "Species",
    global_size_adjust = 1.2
  )
  # You can add other ggplot2 layers, like facets:
  # + ggplot2::facet_wrap(vars(Species), drop = TRUE)
}
}
