% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BetterVis_Network_PPI.R
\name{BetterVis_Network_PPI}
\alias{BetterVis_Network_PPI}
\title{Create a Network Plot with Pie Chart Nodes}
\usage{
BetterVis_Network_PPI(
  nodes_data,
  edges_data,
  fill_vars,
  node_size = c(0.1, 0.3),
  fill_color = NULL,
  line_size = c(1, 3),
  line_color = "grey65",
  node_text_size = 4,
  niter = 800,
  display_border = TRUE,
  group_var = NULL
)
}
\arguments{
\item{nodes_data}{A data frame for the nodes, containing a 'name' column and numeric columns for `fill_vars`.}

\item{edges_data}{A data frame for the edges, with 'from' and 'to' columns matching names in `nodes_data`.}

\item{fill_vars}{A character vector of 2 to 5 column names in `nodes_data` whose values will form the slices of the pie chart nodes.}

\item{node_size}{A numeric vector of length 2 specifying the `min` and `max` scaled size of the nodes.}

\item{fill_color}{(Optional) A vector of colors for the pie chart slices. If `NULL`, a default palette is used.}

\item{line_size}{A numeric vector of length 2 specifying the `min` and `max` scaled width of the edges.}

\item{line_color}{Character string. The color of the edges.}

\item{node_text_size}{Numeric. The font size for node labels.}

\item{niter}{Numeric. The number of iterations for the Fruchterman-Reingold layout algorithm.}

\item{display_border}{Logical. If `TRUE` and `group_var` is provided, displays borders around node groups.}

\item{group_var}{(Optional) A character string for a column name in `nodes_data` used to group nodes with a surrounding border.}
}
\value{
A `ggraph` object.
}
\description{
This function generates a sophisticated network visualization (e.g., for PPI) where
each node is represented as a pie chart. It uses `ggraph` for layout and plotting,
with options to encircle groups of nodes.
}
\examples{
\dontrun{
if (requireNamespace("ggraph", quietly = TRUE) && requireNamespace("ggforce", quietly = TRUE)) {
  # Load example data
  data("BetterVis_Network_PPI_nodes", package = "BetterVis")
  data("BetterVis_Network_PPI_edges", package = "BetterVis")

  # Define a color palette
  ppi_colors <- c('#CD8280', '#54D0B4', '#78C3ED', '#E69F00', '#E98FBD')

  # Generate the network plot
  BetterVis_Network_PPI(
    nodes_data = BetterVis_Network_PPI_nodes,
    edges_data = BetterVis_Network_PPI_edges,
    fill_vars = c("full", "within5", "after5", "early", "late"),
    fill_color = ppi_colors,
    group_var = "manual_grp",
    node_size = c(0.08, 0.25),
    node_text_size = 4,
    line_size = c(0.5, 2.5),
    line_color = "grey65",
    niter = 800,
    display_border = TRUE
  )
  # You can add other ggplot2 layers for further customization
  # + theme(legend.text = element_text(size=12))
}
}
}
