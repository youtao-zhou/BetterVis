% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BetterVis_Venn_UpsetR.R
\name{BetterVis_Venn_UpsetR}
\alias{BetterVis_Venn_UpsetR}
\title{Create a Customizable UpSet Plot}
\usage{
BetterVis_Venn_UpsetR(
  input_list,
  ratio = c(0.7, 0.3),
  point.size = 3,
  line.size = 1,
  y.label = "Intersection Size",
  x.label = "Set Size",
  main.bar.color = "#FCB2AF",
  sets.bar.color = "#9BDFDF",
  matrix.color = "#8C9FCA",
  all_text_adjust = 2
)
}
\arguments{
\item{input_list}{A named list where each element is a vector representing a set.}

\item{ratio}{A numeric vector of length 2 controlling the ratio of the main
intersection size plot to the set size plot. Default is `c(0.7, 0.3)`.}

\item{point.size}{Numeric. The size of the points in the matrix. Default is `3`.}

\item{line.size}{Numeric. The thickness of the lines connecting points in the matrix. Default is `1`.}

\item{y.label}{Character string. The label for the y-axis (intersection size). Default is `"Intersection Size"`.}

\item{x.label}{Character string. The label for the x-axis (set size). Default is `"Set Size"`.}

\item{main.bar.color}{Character string. The color of the main intersection size bars. Default is `"#FCB2AF"`.}

\item{sets.bar.color}{Character string. The color of the set size bars. Default is `"#9BDFDF"`.}

\item{matrix.color}{Character string. The color of the points and lines in the matrix. Default is `"#8C9FCA"`.}

\item{all_text_adjust}{A numeric value for scaling text elements. Can be:
\itemize{
  \item A single numeric value to scale all text uniformly (Default is `2`).
  \item A numeric vector of length 6 to scale elements individually:
    `c(intersection_size_title, intersection_size_tick_labels, set_size_title, set_size_tick_labels, set_names, numbers_above_bars)`.
}}
}
\value{
Prints the UpSet plot to the current graphics device and invisibly returns the UpSet plot object (`grob`).
}
\description{
This function serves as a wrapper for `UpSetR::upset`, providing a convenient
way to create UpSet plots for visualizing set intersections with enhanced
control over colors and text scaling.
}
\examples{
\dontrun{
# Load example data from the package
data("BetterVis_Venn_UpsetR_example", package = "BetterVis")

# Example 1: Simple usage with uniform text scaling
BetterVis_Venn_UpsetR(BetterVis_Venn_UpsetR_example, all_text_adjust = 1.5)

# Example 2: Using a vector for fine-tuned text scaling
BetterVis_Venn_UpsetR(BetterVis_Venn_UpsetR_example, all_text_adjust = c(1.5, 1, 1.5, 1, 2, 1.2))

# Example 3: Customizing colors and labels
BetterVis_Venn_UpsetR(
  input_list = BetterVis_Venn_UpsetR_example,
  ratio = c(0.6, 0.4),
  y.label = "Common Elements",
  x.label = "Total Elements",
  main.bar.color = "skyblue",
  sets.bar.color = "navyblue",
  matrix.color = "red",
  point.size = 5,
  line.size = 2
)
}
}
